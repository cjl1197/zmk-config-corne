# Core Connection and Feature Settings
#CONFIG_BT_CTLR_PHY_2M=n

# Disable the display engine completely since no screens are attached
CONFIG_ZMK_DISPLAY=n

# FIX: Boost radio power slightly to overcome USB/PC noise on the clone board
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# FIX: Force peripheral side to prioritize its BLE link over USB data lines
CONFIG_ZMK_SPLIT_BLE_ROLE_PERIPHERAL=y

# Power Management and Sleep Cycles
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_TIMEOUT=20000
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000

# FIX: Add a tiny 1ms buffer to press debouncing to eliminate electrical noise spikes
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=1
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# FIX: Tighten BLE intervals slightly for a crisper handshake under USB load
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=400
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=16
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=24
CONFIG_BT_PERIPHERAL_PREF_LATENCY=4

# Enable ZMK Studio for Realtime Keymap Updates
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n

# Battery Telemetry for Fedora Monitoring
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING=y
CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_PROXY=y
